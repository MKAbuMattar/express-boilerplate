/** @type {import('prettier').Config} */
const prettierConfig = {
  bracketSpacing: false,
  trailingComma: 'all',
  tabWidth: 2,
  printWidth: 80,
  singleQuote: true,
  semi: true,
};

export default prettierConfig;
